# Tuple is an item in which is immutable i.e, it can't be  changed like list
myTuple=(3,5,2,6,8)
print(myTuple)
print(type(myTuple))
emptyTuple=()
print(emptyTuple)
singleTuple=(4,) # here comma must be added after single element
print(singleTuple)